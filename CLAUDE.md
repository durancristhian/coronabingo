@AGENTS.md

## Agent skills

### Issue tracker

Work is tracked in local Markdown files under `research/`. Before creating, reading, or updating tickets and specs, read `docs/agents/issue-tracker.md`.

### Triage labels

Tickets use the five default triage states. Before assigning a triage state, read `docs/agents/triage-labels.md`.

### Domain docs

Single-context layout: root `CONTEXT.md` and `docs/adr/`. Before exploring the codebase, read `docs/agents/domain.md`.
